# Instalar en un entorno independiente con acceso al SDK del robot.
# Fallar rápido si no hay wheel compatible; no compilar en Reachy.
--only-binary=:all:
reachy-mini>=1.10,<2
numpy>=1.26,<3
opencv-python-headless>=4.12,<5
